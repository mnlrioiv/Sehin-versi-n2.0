@model IEnumerable<Sehin_versión2._0.Models.CondicionIva>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PorcentajeIva)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DiscriminaIva)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LetraFiscal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PorcentajeIva)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DiscriminaIva)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LetraFiscal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
